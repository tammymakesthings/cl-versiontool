(in-package :cl-versiontool/core)

(defun make-app-context (&optional app-config-path version-file-path version-file-type))

(defun setup-app-context (app-config-path))

(defun setup-logger (app-config))
